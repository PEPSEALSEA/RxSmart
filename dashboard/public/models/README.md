# Athlete model

`athlete.glb` is the Mixamo X Bot from the three.js examples (`examples/models/gltf/Xbot.glb`), used for Live IMU game coaching. Replace this file with any Mixamo-rigged humanoid GLB (same bone names) without code changes.
